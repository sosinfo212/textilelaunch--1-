# Copy to .env locally — NEVER commit .env (production uses /opt/textilelaunch/.env on the server)

NODE_ENV=development
PORT=5001

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=textilelaunch_db
DB_PASSWORD=change-me
DB_NAME=agency

SESSION_SECRET=generate-with-openssl-rand-hex-32
FRONTEND_URL=http://localhost:3000
SCRAPER_API_URL=http://localhost:5001/api
COOKIE_SECURE=false
COOKIE_SAMESITE=lax

VITE_API_URL=/api
GEMINI_API_KEY=
